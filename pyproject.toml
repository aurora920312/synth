[project]
name = "agentic"
version = "0.1.0"
description = "A full-featured ReAct-style Python agent with tools and logging"
authors = [{name = "Cursor Agent", email = "agent@example.com"}]
requires-python = ">=3.10"
readme = "README.md"
dependencies = []

[project.scripts]
agentic = "agentic.cli:main"
